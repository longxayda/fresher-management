
export const listBonusPenaltySelector = (state) => state.bonusPenalty.value;

export const isLoadingSelector = (state) => state.bonusPenalty.isLoading;

export const isProcessingSelector = (state) => state.bonusPenalty.isProcessing;

export const exstingSelector = (state) => state.bonusPenalty.exsting;

export const dataSelector = (state) => state.manageFR.value;

export const maxIndexSelector = (state) => state.manageFR.maxIndex;

export const errorSelector = (state) => state.bonusPenalty.error;